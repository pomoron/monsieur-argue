"""Legora negotiation assessor.

The assessor agent grades a junior lawyer's performance in a negotiation
training round against a playbook, produces specific coaching, and computes
the adaptive difficulty for the next round.

Public entry point: ``assessor.cli.main`` (also exposed via the root
``assess.py`` script).
"""

__version__ = "1.0.0"
